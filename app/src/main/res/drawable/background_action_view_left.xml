<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/rippleTransparentWhite30">

    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@color/textPrimaryWhite" />
            <corners
                android:bottomRightRadius="@dimen/actionViewCorners"
                android:topRightRadius="@dimen/actionViewCorners" />
        </shape>
    </item>

    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/colorPrimary" />
            <corners
                android:bottomRightRadius="@dimen/actionViewCorners"
                android:topRightRadius="@dimen/actionViewCorners" />
        </shape>
    </item>

</ripple>