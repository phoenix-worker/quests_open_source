<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/colorPrimary">

    <item
        android:id="@android:id/mask"
        android:bottom="@dimen/tinyMargin"
        android:left="@dimen/tinyMargin"
        android:right="@dimen/tinyMargin"
        android:top="@dimen/tinyMargin">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners
                android:bottomLeftRadius="@dimen/questViewLeftRippleCorners"
                android:bottomRightRadius="@dimen/normalCorners"
                android:topLeftRadius="@dimen/questViewLeftRippleCorners"
                android:topRightRadius="@dimen/normalCorners" />
        </shape>
    </item>

</ripple>